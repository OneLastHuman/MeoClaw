# 调试流程

## 开发模式

### 启动开发服务

```bash
npm run tauri dev
```

Vite 热更新自动生效，前端代码（Vue/TS）修改后无需重启。

### 彻底重启（仅必要时）

以下情况需要手动清理进程后重开：

- Rust 端代码修改
- 端口 5173 被占用
- 多开进程导致冲突

```bash
ps aux | grep "[m]eo-claw" | grep -v "\-\-editor" | awk '{print $2}' | xargs kill -9 2>/dev/null || true
```

---

## 日志追踪

### 启动并记录

```bash
npm run tauri dev 2>&1 | tee /tmp/meo_debug.log &
```

### 实时查看

```bash
tail -f /tmp/meo_debug.log
```

### 前端日志点（`src/components/DesktopPet.vue`）

| 时机 | 日志内容 |
|---|---|
| 动画切换 | `[ANIM] switch to {idle\|EnterInput}` |
| 每帧切换 | `[FRAME] {n}` |
| Timer 启停 | `[TIMER] start / stop` |
| 双击检测 | `[CLICK] double-click, interval=...ms` |
| Response 状态 | `[WINDOW] enter_response: pos=(x,y) size=WxH catY=n` |

---

## 视觉快照

截取桌宠 180x180 主窗口截图：

```bash
screencapture -l $(swift -e '
import CoreGraphics
let w = CGWindowListCopyWindowInfo([.optionAll], 0) as! [[String:Any]]
for x in w {
    let name = x["kCGWindowName"] as? String ?? ""
    let owner = x["kCGWindowOwnerName"] as? String ?? ""
    let bounds = x["kCGWindowBounds"] as? [String:Double] ?? [:]
    let width = bounds["Width"] ?? 0
    let height = bounds["Height"] ?? 0
    if owner == "meo-claw" && name == "MeoClaw" && width == 180 && height == 180 {
        print(x["kCGWindowNumber"]!)
        exit(0)
    }
}
print(0)
' 2>/dev/null) -x /tmp/meo_snap.png
```

快照输出：`/tmp/meo_snap.png`

---

## 进程清理

```bash
# 检查残留进程
ps aux | grep -E "[m]eo-claw" | grep -v grep

# 杀掉所有残留进程
pkill -f "meo-claw"

# 清理端口占用
lsof -ti:5173 | xargs kill -9 2>/dev/null
```

---

## 调试命令参考

| 操作 | 命令 |
|---|---|
| 启动开发 | `npm run tauri dev` |
| 运行测试 | `npm test` |
| 查看日志 | `tail -f /tmp/meo_debug.log` |
| 截取快照 | 见上方 screencapture 命令 |
| 清理进程 | `pkill -f "meo-claw"` |
